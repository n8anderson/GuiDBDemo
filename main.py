import sqlite3
import guiwindow


def main():
    # Main database stuff here
    name = 'Example.db'
    # create_db(name)
    # setup_db(connection, cursor)
    # populate_db(cursor, data)
    # get_info(cursor, id) # May want to consider including this in GUI
    pass
